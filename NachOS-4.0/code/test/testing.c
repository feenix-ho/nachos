#include "syscall.h"

int main()
{
  // int num = ReadNum();
  // PrintNum(num);
  // char chr = ReadChar();
  // PrintChar(chr);
  // int num = RandomNum();
  // PrintNum(num);
  // char str[25];
  // ReadString(str, 24);
  // PrintString(str);
  Ascii();
  Halt();
}
